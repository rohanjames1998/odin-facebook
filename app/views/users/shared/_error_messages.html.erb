<% if resource.errors.any? %>
  <div id="error_explanation" data-turbo-cache="false">
    <h2>
    <%= "#{pluralize(resource.errors.count, "error")} occured while creating your account" %>
    </h2>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
