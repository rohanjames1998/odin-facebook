<% Post.includes(:user).order("created_at DESC").relevant_posts(current_user).each do |post| %>
  <div>
    <div>
      <%= post.user.name %>
      <%= post.created_at%>
    </div>
    <%= post.text_content %>
  </div>
<% end %>
